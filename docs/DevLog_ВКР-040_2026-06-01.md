# DevLog ВКР-040 — Детальная страница заявки клиента
📅 01.06.2026

---

## Phase 1 — Анализ

### Протокол сверки с источниками

| Источник | Результат |
|---|---|
| а) Диаграммы | Project→Comment (related_name='comments'), Project→Attachment (related_name='attachments') — по ER ✅ |
| б) Дизайн-доки | `.badge--{status}`, `var(--color-*)` — переиспользованы. Новые: `.ticket-detail`, `.ticket-meta`, `.comment-card`, `.attachment-item` |
| в) Кодовая база | `get_object_or_404(Project, pk=pk, user=request.user)` — защита от доступа к чужим заявкам. `prefetch_related('comments__user', 'attachments')` — оптимизация запросов. `Attachment.file_path` — CharField, не FileField → показываем имена без скачивания |
| г) ТЗ / 1.4 | F03 (просмотр статуса), F05 (история переписки/комментарии) |
| д) README | Неделя 4 ✅ |
| е) TZ_CHECKLIST | #17б ЛК клиента → 🔄 3/5 страниц |
| ж) ИЗ | Личный кабинет ≥5 страниц — прогресс 3/5 ✅ |

### Проблемы найдены
- `Attachment.file_path` — CharField (не FileField), реальное скачивание невозможно → показываем только имя + тип + размер. Загрузка/скачивание файлов — отдельная задача (критерий #13, 3 балла)

---

## Phase 2 — План (подтверждён)

- `core/urls.py` — +`dashboard/tickets/<int:pk>/`
- `core/views.py` — `ticket_detail_view`: `get_object_or_404` по pk+user, `prefetch_related`
- `templates/core/ticket_detail.html` — шапка (статус-бейдж, мета), описание, вложения, комментарии
- `templates/core/dashboard.html` — ссылки на `ticket_detail` в таблице
- `static/css/style.css` — `.ticket-detail`, `.ticket-meta`, `.attachments-list`, `.comment-card`

---

## Phase 3 — Реализация

### Файлы изменены/созданы

| Файл | Действие |
|---|---|
| `core/urls.py` | +`dashboard/tickets/<int:pk>/` |
| `core/views.py` | +`ticket_detail_view`, +`TICKET_NOT_FOUND_MESSAGE` |
| `templates/core/ticket_detail.html` | Создан |
| `templates/core/dashboard.html` | Строки таблицы → ссылки на detail |
| `static/css/style.css` | +`.ticket-detail`, `.ticket-meta`, `.attachments-list`, `.attachment-item`, `.comment-card`, `.ticket-row__link` |
| `docs/TZ_CHECKLIST.md` | #17б → 3/5 |

### Страницы ЛК клиента после ВКР-040
dashboard · ticket_create · ticket_detail = **3 / 5** ✅

### Проверки
- [x] URL-теги: `{% url 'core:ticket_detail' pk=ticket.pk %}` — OK
- [x] Защита: `get_object_or_404(..., user=request.user)` — чужая заявка → 404 — OK
- [x] prefetch_related — нет N+1 запросов — OK
- [x] Breadcrumbs — OK
- [x] HEX в новых блоках CSS: нет — OK
- [x] Inline-стили: нет — OK

---

## Release
→ Release 0.10 добавлен в `RELEASE_NOTES.md`
