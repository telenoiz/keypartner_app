# DevLog ВКР-043 / ВКР-044 / ВКР-045 — ЛК менеджера + файловая система
📅 03.06.2026

---

## Phase 1 — Анализ

| Источник | Результат |
|---|---|
| а) Диаграммы | Project→Comment, Project→Attachment — по ER. manager FK в Project ✅ |
| б) Дизайн-доки | Переиспользованы `.tickets-table`, `.badge`, `.stat-card`, `.comment-card`. Новые: `.manager-detail-layout`, `.filter-bar`, `.upload-form`, `.stats-layout` |
| в) Кодовая база | `user.is_manager` / `user.is_admin` — property-методы ✅. `Attachment.file_path` → заменено на `FileField` |
| г) ТЗ / 1.4 | F06 (список заявок менеджера), F07 (смена статуса + комментарий), F08 (назначение менеджера авто при смене статуса) |
| ж) ИЗ | ЛК менеджера ≥5 страниц ✅. Доступ к файловой системе ✅ |
| е) TZ_CHECKLIST | #13 (файлы, 3б) → ✅; #17 (ЛК менеджера, 5б) → ✅; счёт 41→49 |

### Проблемы найдены
- `Attachment.file_path` — CharField, не FileField → заменено, нужна миграция (**разрешено в Phase 3**)

---

## Phase 3 — Реализация

### Файлы изменены/созданы

| Файл | Действие |
|---|---|
| `core/forms.py` | +`TicketFilterForm`, `TicketStatusForm`, `CommentForm`, `AttachmentUploadForm` |
| `core/models.py` | `Attachment.file_path` → `FileField(upload_to='attachments/%Y/%m/')` |
| `core/views.py` | +`manager_required` декоратор; +`manager_dashboard_view`, `manager_ticket_detail_view`, `manager_stats_view`; +`attachment_upload_view`, `attachment_download_view` |
| `core/urls.py` | +5 маршрутов: manager_dashboard, manager_ticket_detail, manager_stats, attachment_upload, attachment_download |
| `keypartner/urls.py` | +`static(MEDIA_URL, ...)` для DEBUG |
| `templates/core/manager_dashboard.html` | Создан |
| `templates/core/manager_ticket_detail.html` | Создан |
| `templates/core/manager_stats.html` | Создан |
| `templates/core/ticket_detail.html` | +кнопки скачивания + форма загрузки для клиента |
| `static/css/style.css` | +`.manager-detail-layout`, `.filter-bar`, `.upload-form`, `.stats-layout`, `.stats-card` |
| `docs/TZ_CHECKLIST.md` | #13, #17 → ✅, счёт 49/57 |

### ЛК менеджера — 5 страниц
manager_dashboard · manager_ticket_detail · manager_stats · profile · notifications = **5/5** ✅

### Проверки
- [x] `manager_required`: проверка is_manager || is_admin — OK
- [x] `attachment_download_view`: проверка владельца для клиента — OK
- [x] `manager_ticket_detail_view`: action='status' / action='comment' — два независимых POST — OK
- [x] `FileResponse` с `as_attachment=True` — OK
- [x] URL-теги: только `{% url 'core:...' %}` — OK
- [x] CSRF: все POST-формы — OK
- [x] Breadcrumbs: все три шаблона — OK

### ⚠️ Требуется миграция
```bash
python manage.py makemigrations
python manage.py migrate
```

---

## Release
→ Release 0.13–0.15 добавлены в `RELEASE_NOTES.md`
