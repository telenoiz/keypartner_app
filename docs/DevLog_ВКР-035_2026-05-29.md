# DevLog ВКР-035 — Контакты + форма ContactMessage
📅 29.05.2026

---

## Phase 1 — Анализ

### Протокол сверки с источниками

| Источник | Результат |
|---|---|
| а) Диаграммы (TO_BE, AS_IS, ER) | ContactMessage присутствует в ER; связь с User через FK (SET_NULL) — подтверждено |
| б) Дизайн-доки (2.1_Color_Typography) | CSS-токены использованы, Heroicons не требуются, шрифт Inter из base.html |
| в) Кодовая база | `contacts_view` — стаб; `ContactMessage` модель готова; `base.html` — container уже в main |
| г) ТЗ | §3: breadcrumbs на каждой странице (отсутствовали — **КРИТИЧНО**); §9: ФИО в footer (отсутствовало); Форма обратной связи = 1 балл |
| д) README/ИЗ | Выбран стек Python/Django; требования ТЗ §3, §9 покрыты в этом коммите |

### Проблемы найдены
1. `contacts_view` — стаб, не обрабатывает POST (**Critical**)
2. Нет `ContactMessageForm` (**Critical**)
3. Breadcrumbs отсутствовали во всех 8 шаблонах (**Critical**, ТЗ §3)
4. ФИО автора отсутствовало в footer (**High**, ТЗ §9)
5. Авторизованный пользователь не получал pre-fill (**High**)

---

## Phase 2 — План (подтверждён)

- `core/forms.py` (new) — `ContactMessageForm`
- `core/context_processors.py` (new) — `site_meta` → `SITE_AUTHOR`
- `core/views.py` — реализован `contacts_view` GET+POST
- `keypartner/settings.py` — context processor + `SITE_AUTHOR`
- `templates/base.html` — `{% block breadcrumbs %}` + footer ФИО
- Все 8 шаблонов — breadcrumbs
- `static/css/style.css` — `.breadcrumb`, `.contacts-layout`, `.form-row`

---

## Phase 3 — Реализация

### Файлы изменены/созданы

| Файл | Действие | Ключевые изменения |
|---|---|---|
| `core/forms.py` | Создан | `ContactMessageForm`, `PHONE_REGEX`, `MESSAGE_MIN_LENGTH=10` |
| `core/context_processors.py` | Создан | `site_meta` → `SITE_AUTHOR` |
| `core/views.py` | Изменён | `contacts_view` GET/POST; константа `CONTACT_SUCCESS_MESSAGE` |
| `keypartner/settings.py` | Изменён | context processor; `SITE_AUTHOR = config(...)` |
| `templates/base.html` | Изменён | `{% block breadcrumbs %}`; `{{ SITE_AUTHOR }}` в footer |
| `templates/core/contacts.html` | Изменён | Полный шаблон: hero + 2-col layout + форма |
| `static/css/style.css` | Изменён | `.breadcrumb`, `.contacts-layout`, `.form-row` и др. |
| `templates/core/*.html` (×7) | Изменены | `{% block breadcrumbs %}` с хлебными крошками |

### Проверки
- [x] AST-анализ: `forms.py`, `views.py`, `context_processors.py`, `settings.py` — OK
- [x] Hardcode: нет raw HEX в компонентных блоках CSS — OK
- [x] URL-теги: все ссылки через `{% url %}` — OK
- [x] Breadcrumbs: все 8 шаблонов — OK
- [x] CSRF: `{% csrf_token %}` в форме — OK
- [x] Redirect-after-POST: `redirect(reverse('core:contacts'))` — OK

### Константы
| Константа | Значение | Файл |
|---|---|---|
| `PHONE_REGEX` | `r'^\+?[\d\s\-() ]{7,20}$'` | `forms.py` |
| `MESSAGE_MIN_LENGTH` | `10` | `forms.py` |
| `CONTACT_SUCCESS_MESSAGE` | строка | `views.py` |
| `SITE_AUTHOR` | `config('SITE_AUTHOR', ...)` | `settings.py` |

---

## Release
→ Release 0.6 добавлен в `RELEASE_NOTES.md`
