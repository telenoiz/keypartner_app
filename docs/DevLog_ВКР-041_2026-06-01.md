# DevLog ВКР-041 — Профиль пользователя + Уведомления
📅 01.06.2026

---

## Phase 1 — Анализ

### Протокол сверки с источниками

| Источник | Результат |
|---|---|
| а) Диаграммы | Notification → User (FK, related_name='notifications'), поля message/is_read/created_at/project — по ER ✅ |
| б) Дизайн-доки | `.card`, `.btn-outline`, `.btn-sm`, `var(--color-*)` — есть. Новые: `.profile-layout`, `.profile-sidebar`, `.notifications-list`, `.notification-item--unread` |
| в) Кодовая база | User.fields: first_name, last_name, email, username, created_at ✅. `Notification.objects.filter(user=request.user)` — прямой запрос. `notification_mark_read_view` — `@require_POST` + owner-check |
| г) ТЗ / 1.4 | F01 (редактирование профиля), F04 (уведомления при изменении статуса) |
| д) README | Неделя 4 ✅ |
| е) TZ_CHECKLIST | #17б ЛК клиента → ✅ 5/5, счёт 36→41 (+5 баллов) |
| ж) ИЗ | «Личный кабинет ≥5 страниц» — критерий закрыт полностью ✅ |

### Проблемы найдены
Нет.

---

## Phase 2 — План (подтверждён)

- `core/forms.py` — `ProfileUpdateForm`: first_name/last_name/email, clean_email с exclude(pk=self.instance.pk)
- `core/views.py` — `profile_view` GET+POST; `notifications_view` (Paginator, order by is_read); `notification_mark_read_view` (@require_POST, owner-check)
- `core/urls.py` — +profile/, +notifications/, +notifications/<pk>/read/
- `templates/core/profile.html` — sidebar с аватаром и nav, форма редактирования, readonly логин
- `templates/core/notifications.html` — список с бейджем «непрочитано», кнопка POST «Прочитано», empty state
- `static/css/style.css` — `.profile-layout`, `.profile-sidebar`, `.profile-avatar`, `.profile-nav`, `.notifications-list`, `.notification-item`, `.visually-hidden`

---

## Phase 3 — Реализация

### Файлы изменены/созданы

| Файл | Действие |
|---|---|
| `core/forms.py` | +`ProfileUpdateForm`, +`ERR_EMAIL_EXISTS_OTHER` |
| `core/views.py` | +`profile_view`, +`notifications_view`, +`notification_mark_read_view`; +`Notification` импорт; +2 константы |
| `core/urls.py` | +3 маршрута |
| `templates/core/profile.html` | Создан |
| `templates/core/notifications.html` | Создан |
| `static/css/style.css` | +`.profile-*`, `.notifications-*`, `.visually-hidden` |
| `docs/TZ_CHECKLIST.md` | #17б → ✅, счёт 41/57 |

### Страницы ЛК клиента — итог
dashboard · ticket_create · ticket_detail · profile · notifications = **5 / 5** ✅

### Проверки
- [x] URL-теги: только `{% url 'core:...' %}` — OK
- [x] `notification_mark_read_view`: `@require_POST` + `get_object_or_404(..., user=request.user)` — OK
- [x] `profile_view`: `exclude(pk=self.instance.pk)` в clean_email — не блокирует сохранение своего же email — OK
- [x] CSRF в forms (profile + notification mark read) — OK
- [x] Redirect-after-POST в обоих POST-views — OK
- [x] HEX в новых блоках CSS: нет — OK
- [x] Breadcrumbs: оба шаблона — OK

---

## Release
→ Release 0.11 добавлен в `RELEASE_NOTES.md`
