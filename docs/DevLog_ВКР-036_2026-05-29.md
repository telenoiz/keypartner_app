# DevLog ВКР-036 — Логин, регистрация, 404
📅 29.05.2026

---

## Протокол сверки с источниками

| Источник | Результат |
|---|---|
| а) Диаграммы/ER | User → Role (FK, name='client/manager/admin'), поля username/email — подтверждено |
| б) Дизайн-доки | Токены, `.form-input`, `.btn-primary`, `.alert--danger` — все есть |
| в) Кодовая база | Стабы `login_view`/`register_view`, `ROLE_CLIENT='client'`, `USERNAME_FIELD='username'` |
| г) ТЗ/ИЗ | F01: регистрация и авторизация по логину/паролю; 3 роли (+3 балла ТЗ); после 404 = 9 страниц (нужна ещё 1 для ≥10) |
| д) README/ИЗ | Python/Django, ≥50 коммитов |

**Tech debt зафиксирован:** после ВКР-036 страниц 9/10. Нужна ещё одна (ВКР-037 dashboard добавит несколько).

---

## Реализовано

| Файл | Изменения |
|---|---|
| `core/forms.py` | `LoginForm` (authenticate в clean), `RegisterForm` (уникальность, min length, пароли) |
| `core/views.py` | `login_view`, `register_view` (GET/POST), `handler404_view` |
| `keypartner/urls.py` | `handler404` |
| `templates/core/login.html` | auth-card, non_field_errors, next |
| `templates/core/register.html` | auth-card--wide, 2-col form-row |
| `templates/404.html` | error-page |
| `static/css/style.css` | .auth-card, .btn--full, .error-page |

## Проверки
- [x] Импорты: дублирующий `login as auth_login` — исправлен (один import)
- [x] CSRF в обеих формах
- [x] Role.objects.get(name=ROLE_CLIENT) — не полагаемся на default=3
- [x] Redirect-after-login через `?next=`
- [x] 404 зарегистрирован в urls.py

## Release
→ Release 0.7 добавлен в `RELEASE_NOTES.md`
