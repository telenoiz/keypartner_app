# DevLog ВКР-038 — Дашборд клиента: список заявок + создание заявки
📅 01.06.2026

---

## Phase 1 — Анализ

### Протокол сверки с источниками

| Источник | Результат |
|---|---|
| а) Диаграммы | Project→User (FK, related_name='tickets'); поля status/title/service/priority/created_at — всё по ER ✅ |
| б) Дизайн-доки | `.card`, `.badge--{status}`, `.btn-primary`, `var(--badge-*-border)` — переиспользованы. Новые: `.dashboard-stats`, `.stat-card`, `.tickets-table`, `.pagination`, `.form-actions` |
| в) Кодовая база | `Project.STATUS_NEW/IN_PROGRESS/RESOLVED/CLOSED` — class-level constants ✅. `related_name='tickets'` — не использован, явный filter(user=request.user) надёжнее. `dashboard.html` — inline-стиль `style="color:..."` убран ✅ |
| г) ТЗ / 1.4 | F02 (создание заявки), F03 (просмотр статусов). Критерий #17 — ЛК клиента, 2/5 страниц |
| д) README | Неделя 4 (1–7 июня). ВКР-038 первый коммит понедельника |
| е) TZ_CHECKLIST | #17 → 🔄 (2/5 страниц ЛК). Регрессий нет |

### Проблемы найдены
- `dashboard.html` (старый) содержал inline `style="color:..."` — убрано при переписи ✅

---

## Phase 2 — План (подтверждён)

- `core/forms.py` — `ProjectCreateForm`: поля title/description/service/priority; `TITLE_MIN_LENGTH = 5`; queryset только активных услуг
- `core/views.py` — реализован `dashboard_view` (Paginator, stat-counters); новый `ticket_create_view` GET+POST; константы `DASHBOARD_TICKETS_PER_PAGE`, `TICKET_CREATE_SUCCESS_MESSAGE`
- `core/urls.py` — `+path('dashboard/tickets/new/', ...)`
- `templates/core/dashboard.html` — переписан: заголовок+CTA, 4 stat-cards, таблица заявок с бейджами, пагинация, empty state
- `templates/core/ticket_create.html` — создан: форма с title/service/priority/description, 2-col form-row, CSRF, кнопка Отмена
- `static/css/style.css` — `.dashboard-*`, `.stat-card`, `.tickets-table`, `.ticket-row`, `.pagination`, `.ticket-form-card`, `.form-actions`

---

## Phase 3 — Реализация

### Файлы изменены/созданы

| Файл | Действие |
|---|---|
| `core/forms.py` | +`ProjectCreateForm`, импорты Project/Service/Priority, `TITLE_MIN_LENGTH` |
| `core/views.py` | `dashboard_view` полная; +`ticket_create_view`; +Paginator импорт; +2 константы |
| `core/urls.py` | +`dashboard/tickets/new/` |
| `templates/core/dashboard.html` | Полная перепись |
| `templates/core/ticket_create.html` | Создан |
| `static/css/style.css` | +9 новых блоков |

### Страницы ЛК клиента после ВКР-038
`/dashboard/` + `/dashboard/tickets/new/` = **2 / 5** страниц ЛК

### Проверки
- [x] URL-теги: только `{% url 'core:...' %}` — OK
- [x] Inline-стили в шаблонах: нет — OK
- [x] HEX в новых блоках CSS: нет, только `var(--color-*)` и `var(--badge-*)` — OK
- [x] CSRF в `ticket_create.html` — OK
- [x] Redirect-after-POST → `core:dashboard` — OK
- [x] `ticket.status = Project.STATUS_NEW` — константа, не строка — OK
- [x] Breadcrumbs: оба шаблона — OK

---

## Release
→ Release 0.9 добавлен в `RELEASE_NOTES.md`
